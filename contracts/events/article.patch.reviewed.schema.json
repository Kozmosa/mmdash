{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmdash.moe/contracts/events/article.patch.reviewed.schema.json",
  "title": "article.patch.reviewed payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["patch_id", "status"],
  "properties": {
    "patch_id": { "type": "string", "format": "uuid" },
    "accepted_revision": { "type": "integer", "minimum": 1 },
    "status": { "enum": ["accepted", "rejected"] }
  }
}

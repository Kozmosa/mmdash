{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmdash.dev/contracts/events/agent.session.default_changed.schema.json",
  "title": "agent.session.default_changed payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["project_id", "resource_id", "agent_instance_id"],
  "properties": {
    "project_id": { "type": "string", "format": "uuid" },
    "resource_id": { "type": "string", "format": "uuid" },
    "agent_instance_id": { "type": "string", "format": "uuid" }
  }
}

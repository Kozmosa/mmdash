{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmdash.dev/contracts/events/progress.evaluation.started.schema.json",
  "title": "progress.evaluation.started payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["resource_id", "resource_type", "job_id", "attempt"],
  "properties": {
    "resource_id": { "type": "string", "format": "uuid" },
    "resource_type": { "const": "progress_evaluation" },
    "job_id": { "type": "string", "format": "uuid" },
    "attempt": { "type": "integer", "minimum": 0 }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmdash.dev/contracts/events/agent.instance.revoked.schema.json",
  "title": "agent.instance.revoked payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["project_id", "resource_id", "status"],
  "properties": {
    "project_id": { "type": "string", "format": "uuid" },
    "resource_id": { "type": "string", "format": "uuid" },
    "status": { "const": "disabled" }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmdash.moe/schemas/mcp-tools/data.read.json",
  "title": "data.read MCP tool",
  "description": "Reads authoritative content for one decision-relevant Data Hub object selected through data.list.",
  "x-operation-id": "data.read",
  "$defs": {
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["project_id", "object_id"],
      "properties": {
        "project_id": {
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{1,127}$",
          "description": "Exact Project ID used in the preceding data.list call."
        },
        "object_id": {
          "type": "string",
          "format": "uuid",
          "description": "Object ID selected from data.list because its full content can answer a material question."
        }
      }
    },
    "output": {
      "type": "object",
      "required": ["object", "content"],
      "properties": {
        "object": {
          "type": "object"
        },
        "content": {}
      }
    }
  }
}

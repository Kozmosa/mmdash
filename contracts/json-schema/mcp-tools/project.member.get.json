{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmdash.moe/schemas/mcp-tools/project.member.get.json",
  "title": "project.member.get MCP tool",
  "description": "Reads one project member and role.",
  "x-operation-id": "project.member.get",
  "$defs": {
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["project_id", "user_id"],
      "properties": {
        "project_id": {
          "type": "string",
          "format": "uuid"
        },
        "user_id": {
          "type": "string",
          "format": "uuid"
        }
      }
    },
    "output": { "type": "object" }
  }
}

# API Types

Stable shared DTO, pagination, error, ID, timestamp, and Event Envelope types.
`src/generated.ts` comes from the JSON Schemas in `contracts/`; regenerate it
with `pnpm contracts:generate` and never edit it directly.

# Validation

Shared JSON Schema 2020-12 validation and normalized issue formatting. Use
`createSchemaValidator<T>()` with referenced schemas explicitly supplied.
Provider-specific input normalization remains in its owning module.
